/**
 * Generated by a build script. Do not modify.
 * Check orginal .jsx file.
 */
/* eslint-disable */

(function(Drupal2, wp2, drupalSettings2) {
  function registerCoreBlocks() {
    const { blocks, blockLibrary } = wp2;
    const {
      setDefaultBlockName,
      setGroupingBlockName,
      setUnregisteredTypeHandlerName,
      unregisterBlockVariation
    } = blocks;
    const { __experimentalGetCoreBlocks } = blockLibrary;
    const {
      allowedBlocks
    } = drupalSettings2.editor.formats.gutenberg.editorSettings;
    const defaultBlocks = [
      "core/block",
      "core/heading",
      "core/list",
      "core/list-item",
      "core/paragraph",
      "core/pattern",
      "core/missing"
    ];
    const coreBlocks = __experimentalGetCoreBlocks();
    const allowedCoreBlocks = coreBlocks.filter(
      (block) => allowedBlocks && allowedBlocks.hasOwnProperty(block.name) && allowedBlocks[block.name] || defaultBlocks.includes(block.name)
    );
    allowedCoreBlocks.forEach((block) => {
      block.init();
    });
    setUnregisteredTypeHandlerName("core/missing");
    setDefaultBlockName("core/paragraph");
    setGroupingBlockName("core/group");
    if (allowedBlocks) {
      Object.keys(allowedBlocks).forEach((key) => {
        const value = allowedBlocks[key];
        if (key.startsWith("core-embed") && !value) {
          unregisterBlockVariation("core/embed", key.split("core-embed/")[1]);
        }
      });
    }
  }
  function __experimentalRegisterExperimentalCoreBlocks() {
    return null;
  }
  wp2.blockLibrary = {
    ...wp2.blockLibrary,
    registerCoreBlocks,
    __experimentalRegisterExperimentalCoreBlocks
  };
})(Drupal, wp, drupalSettings);
